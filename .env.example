# Environment Configuration for Macro Sentiment Trading Pipeline

# ==============================================
# GDELT Data Collection Configuration
# ==============================================

# Choose data collection method: 'free' or 'bigquery'
GDELT_METHOD=free

# ==============================================
# Google BigQuery Configuration (Optional)
# ==============================================
# Required only if GDELT_METHOD=bigquery

# Google Cloud Project ID
# Get this from: https://console.cloud.google.com
GOOGLE_CLOUD_PROJECT=your-project-id-here

# Path to Google Cloud Service Account JSON credentials
# Download from: Cloud Console > IAM & Admin > Service Accounts
GOOGLE_APPLICATION_CREDENTIALS=path/to/your/service-account-key.json

# Alternative: You can also use OAuth instead of service account
# In that case, leave GOOGLE_APPLICATION_CREDENTIALS empty and run:
# gcloud auth application-default login

# ==============================================
# BigQuery Cost Management
# ==============================================
# Optional: Set a query cost limit (in USD) to prevent unexpected charges
BIGQUERY_MAX_COST_USD=1.00

# ==============================================
# Other API Keys (Add as needed)
# ==============================================

# News API (if using alternative news sources)
# NEWS_API_KEY=your-news-api-key

# Alpha Vantage (for market data)
# ALPHA_VANTAGE_API_KEY=your-alpha-vantage-key

# ==============================================
# Database Configuration (Optional)
# ==============================================

# PostgreSQL connection (if storing results in database)
# DATABASE_URL=postgresql://username:password@localhost:5432/macro_sentiment

# ==============================================
# Logging and Debug Configuration
# ==============================================

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Enable detailed BigQuery query logging
BIGQUERY_DEBUG=false

# ==============================================
# LLM-Enhanced Sentiment Analysis Configuration
# ==============================================

# API Provider Enable/Disable Flags
LLM_USE_OPENAI=true
LLM_USE_ANTHROPIC=true
LLM_USE_LOCAL=true
LLM_FALLBACK_FINBERT=true

# API Keys (get from respective provider dashboards)
OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Model Selection (cost-effective defaults)
LLM_OPENAI_MODEL=gpt-4o-mini
LLM_ANTHROPIC_MODEL=claude-3-haiku-20240307

# Performance and Cost Control Settings
LLM_BATCH_SIZE=8
LLM_CONFIDENCE_THRESHOLD=0.7
LLM_CACHE_DIR=data/cache/llm_sentiment
LLM_MAX_REQUESTS_PER_MINUTE=60
LLM_REQUEST_TIMEOUT=30
LLM_MAX_TOKENS=200
LLM_TEMPERATURE=0.1

# Model Options:
# OpenAI: gpt-4o-mini (cheapest), gpt-3.5-turbo, gpt-4o, gpt-4
# Anthropic: claude-3-haiku-20240307 (cheapest), claude-3-sonnet-20240229, claude-3-opus-20240229